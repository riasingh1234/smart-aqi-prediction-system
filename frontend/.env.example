# Point the dashboard at your Flask backend.
# Local dev default (used automatically if this file isn't copied to .env):
VITE_API_URL=http://localhost:5000

# Production example (after deploying backend to Render):
# VITE_API_URL=https://smart-aqi-backend.onrender.com
